let R, // canvas context
    G, // Game instance
    w = window,
    DOODLE_RNG,
    DETAILS_RNG,
    CANVAS = nomangle(g)
    DIFFICULTY_INSTRUCTION = nomangle('Press [K] to change difficulty at any time');
